# Trading Council — Chief of Staff (CoS) Model Configuration
# All parameters locked by Board decision. Do not modify without Board approval.

vocab_size: 25000          # locked — TDST-4
n_layers: 24               # locked — TDST-4
n_heads: 12                # locked — TDST-4
hidden_dim: 768            # locked — TDST-4
ffn_dim: 3072              # approved — 4x hidden_dim (standard ratio)
dropout: 0.1               # approved — consensus default
activation: gelu           # approved — proven, battle-tested
positional_encoding: rope  # approved — zero extra params, generalises to 8192+
context_window: 8192       # locked — Board decision
weight_tying: true         # output projection shares input embedding weights
